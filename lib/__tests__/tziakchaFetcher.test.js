const assert = require('assert');
const tziakchaFetcher = require('../index.js');

describe('tziakchaFetcher', () => {
  it('has a test', () => {
    assert(false, 'tziakchaFetcher should have a test');
  });
});
